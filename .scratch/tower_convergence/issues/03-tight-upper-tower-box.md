# 03: UpperTowerBox 紧致矩形体收敛与非挂点出线导线阻断

**What to build:** 废除顺线厚度 5.5m 硬编码下限，顺线厚度紧密贴合绝缘子挂点端部（$3.0\text{m} \sim 4.2\text{m}$）；横担半宽取消 18% 冗余外推，采用紧致的分位数加安全制造公差（99% + 0.35m）；彻底解决 0-1# 图 1 红框导线染蓝问题，保证上部为纯正长方体 OBB，同时自然阻断外侧跨中导线。

**Blocked by:** 02: FrustumFirstAnchor 四棱台自底向上定姿与全塔单一刚体朝向锁定

**Status:** closed

- [x] 移除 `half_line_t` 中 `max(..., 5.5)` 的硬编码下限
- [x] 基于绝缘子端部真实分布自适应约束顺线半厚度在 3.0m~4.2m（0-1# Tower 1 实测 4.20m，Tower 2 实测 4.02m）
- [x] 优化横担半宽外推裕量，改为紧致的 99 分位数加 0.35m 结构安装公差
- [x] 确保 UpperTowerBox 为纯正定向长方体（OBB）
- [x] 单元测试与端到端实测验证：验证横担端部出线导线不被误入 UpperTowerBox
